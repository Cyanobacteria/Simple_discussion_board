<h1>討論板  <%= @group.name %></h1>
<a href="<%= publicgroups_path %>">返回</a>
</br>
<table class="table">
  <% @posts.each do |post| %> 
  <tr>
  <td><a href="<%= publicpost_path(@group, :post_id => post) %>"><%= post.title%></a></td>
  <tr>
  <% end %>
</table>
