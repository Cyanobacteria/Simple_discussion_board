
<h1>文章名稱  <%= @post.title %></h1>
<a href="<%= publicgroup_path(@post.group) %>">返回</a>
</br>
<table class="table">
  <% @discussions.each do |discussion| %> 
  <tr>
  <td><a href="<%# publicpost_path(@group, :post_id => post) %>"><%= discussion.content%></a></td>
  <tr>
  <% end %>
</table>
