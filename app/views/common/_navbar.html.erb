

<nav class="navbar navbar-inverse">
  <div class="container-fluid">
    <div class="navbar-header">
  <!--這邊有問題要修 -->
     <% if current_user.profile == nil %>
       <a class="navbar-brand" href="<%= mygroups_path%>"><%= current_user.email%></a>
     <% else %>
       <% if current_user.profile.name != nil%>
         <a class="navbar-brand" href="<%= mygroups_path %>"><%= current_user.profile.name%></a>
       <% else %>
         <a class="navbar-brand" href="<%= mygroups_path %>"><%= current_user.email%></a>
       <%end%>
     <% end %>
  <!-- end-->
    </div>
    <ul class="nav navbar-nav">
      <li class="active"><a href="<%= groups_path %>">Home</a></li>
      <li class="dropdown"><a class="dropdown-toggle" data-toggle="dropdown" href="#">使用者功能<span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><a href="<%= profiles_path %>">所有使用者</a></li>
          <li><a href="/profiles/<%= current_user.profile.id %>">我的檔案</a></li>
          <li><a href="#">未設置</a></li>
        </ul>
      </li>
      <li><a href="#">未設置</a></li>
    </ul>
    <ul class="nav navbar-nav navbar-right">
      <!--
      <li><a href="#"><span class="glyphicon glyphicon-user"></span> Sign Up</a></li>
      <li><a href="#"><span class="glyphicon glyphicon-log-in"></span> Login</a></li>
      -->
      <li><%= link_to('Logout', destroy_user_session_path, method: :delete) %> </li>
    </ul>
  </div>
</nav>

